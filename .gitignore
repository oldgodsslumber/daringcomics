# firebase-config.js IS committed — see MULTIPLAYER.md §1. A public web app has
# to ship its Firebase config to every visitor, so hiding it buys nothing; the
# controls are the API-key restrictions and the Realtime Database rules.
# Keep that key restricted (HTTP referrers + API allowlist) at all times.

node_modules/
